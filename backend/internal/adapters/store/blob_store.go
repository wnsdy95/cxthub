package store

// PostgresBlobStore stub removed.
//
// BlobStore is now implemented as a single type with FSStore (fs_store.go, default build) and PostgresStore (postgres_store.go, //go:build postgres) alongside MetadataStore (meta+body same backend).
