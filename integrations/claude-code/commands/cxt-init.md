# /cxt-init

Initialize CXTHub in the current Git repository.

**Execution**: explicit `cxt` CLI

Usage: `/cxt-init [--remote <workspace-url>] [--no-hooks]`. Run `cxt init`
through the shell with the requested flags and report the result. Initialization
changes local state and is not an MCP operation.
