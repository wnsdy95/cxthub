# /cxt-push

Synchronize local CXTHub snapshots and refs to the configured origin.

**Execution**: explicit `cxt` CLI

Usage: `/cxt-push [--force|--append]`. Run `cxt push` through the shell only
because the user explicitly invoked this command, then report synchronization
and ref-convergence results. MCP has no push tool.
