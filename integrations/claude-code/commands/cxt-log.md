# /cxt-log

Alias for `/cxt-list`.

**Execution**: MCP read via `context_list`

Interpret `$ARGUMENTS` as an optional branch and limit, call `context_list`,
and present the saved context commits without changing state.
