# /cxt-search

Search synchronized team context for the current repository.

**Execution**: MCP read via `context_search`

Pass the complete `$ARGUMENTS` text as the query. It must contain at least two
characters. Present matching commits and snippets without changing state.
