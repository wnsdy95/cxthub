# /prompts:cxt-pull

Fetch and reconcile CXTHub snapshots and refs from the configured origin.

**Execution**: explicit `cxt` CLI

Usage: `/prompts:cxt-pull [--force]`. Run `cxt pull` through the shell and
report the resulting local context state. MCP is read-only and has no pull
tool.
